# .env.example - Environment variables template
# Copy this file to .env.local and fill in your values

# Vercel KV (Redis) Configuration
# These are automatically set when you create a KV store in Vercel
# Get these from: Vercel Dashboard → Storage → KV → Your Database → .env.local tab
KV_URL="redis://default:your-kv-password@your-kv-region-redis-hash.upstash.io:6379"
KV_REST_API_URL="https://your-kv-region-redis-hash.upstash.io"
KV_REST_API_TOKEN="your-kv-rest-api-token-here"

# Edge Config (Optional)
# Create at: Vercel Dashboard → Storage → Edge Config
# Leave empty if not using Edge Config
EDGE_CONFIG=""

# API Configuration (Optional)
# Customize API behavior
API_RATE_LIMIT=1000              # Requests per minute per IP
CACHE_TTL=60                     # Default cache TTL in seconds
MAX_FILE_SIZE=10485760          # Max file upload size in bytes (10MB)
ALLOWED_ORIGINS="*"             # CORS allowed origins

# Development Settings
NODE_ENV=development            # development | production | test
VERCEL_ENV=development         # Set automatically by Vercel
VERCEL_URL=""                  # Set automatically by Vercel

# Security (Production Only)
# Add these for enhanced security in production
# RATE_LIMIT_SECRET="your-rate-limit-secret-key"
# API_SECRET_KEY="your-api-secret-key-for-admin-operations"

# Analytics & Monitoring (Optional)
# ANALYTICS_ENABLED=true
# SENTRY_DSN="your-sentry-dsn-for-error-tracking"

# Custom Domain (Optional)
# CUSTOM_DOMAIN="api.yourdomain.com"

# =============================================================================
# SETUP INSTRUCTIONS:
# =============================================================================
# 
# 1. Create Vercel KV Store:
#    - Go to https://vercel.com/dashboard
#    - Navigate to Storage → KV
#    - Click "Create Database"
#    - Copy the environment variables to this file
#
# 2. Pull environment variables (recommended):
#    npx vercel env pull .env.local
#
# 3. Manual setup (alternative):
#    - Copy this file: cp .env.example .env.local
#    - Fill in the KV_* variables from your Vercel KV dashboard
#    - Customize other settings as needed
#
# 4. Test your setup:
#    npm run dev
#    curl http://localhost:3000/api/health
#
# =============================================================================

# Notes:
# - Never commit .env.local to version control
# - KV variables are automatically injected in Vercel deployments
# - Use 'vercel env' command to manage production environment variables
# - Edge Config is optional but recommended for configuration data